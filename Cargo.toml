[package]
name = "schema"
version = "0.0.0"
edition = "2024"

[features]
default = ["std"]
std = ["serde/std"]

[dependencies]
serde = { version = "1.0.228", default-features = false, features = ["derive"] }

[dev-dependencies]
postcard = "1.1.3"
rstest = "0.26.1"
serde_json = "1.0.149"
